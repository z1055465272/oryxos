<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.oryxos</groupId>
    <artifactId>oryxos</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>OryxOS</name>
    <description>面向企业（尤其严监管企业）的私有可审计 Agent OS 统一底座：JDK 21 + Spring Boot 3.x + Spring AI + 自实现 ReAct loop + SQLite + Picocli</description>

    <modules>
        <module>oryxos-core</module>
        <module>oryxos-provider</module>
        <module>oryxos-memory</module>
        <module>oryxos-tool</module>
        <module>oryxos-web</module>
        <module>oryxos-channel-cli</module>
        <module>oryxos-storage</module>
        <module>oryxos-cli</module>
        <module>oryxos-boot</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Spring AI 1.0.x 线（配合 Spring Boot 3.x） -->
        <spring-ai.version>1.0.9</spring-ai.version>
        <!-- Spring AI Alibaba：DashScope/Qwen 等国产模型 connector，US-1 接入 Qwen 时启用 -->
        <spring-ai-alibaba.version>1.1.2.3</spring-ai-alibaba.version>
        <sqlite-jdbc.version>3.53.2.1</sqlite-jdbc.version>
        <picocli.version>4.7.7</picocli.version>
        <mcp-sdk.version>2.0.0</mcp-sdk.version>

        <!-- 工程地基：日志 / 监控 / API 规范 -->
        <springdoc.version>2.8.17</springdoc.version>
        <logstash-logback-encoder.version>9.0</logstash-logback-encoder.version>

        <!-- 工程地基：开发规范与代码安全检查（版本相互锁定，勿随意升级）
             p3c-pmd 编译依赖 PMD 6（2.x 用 6.15.0），因此必须固定 maven-pmd-plugin 3.21.0（PMD 6.55）。
             maven-pmd-plugin ≥3.22 已切 PMD 7，P3C 规则集在 PMD 7 下无法加载。 -->
        <spotless.version>3.9.0</spotless.version>
        <google-java-format.version>1.36.1</google-java-format.version>
        <checkstyle.version>3.6.0</checkstyle.version>
        <p3c-pmd.version>2.1.1</p3c-pmd.version>
        <pmd-plugin.version>3.21.0</pmd-plugin.version>
        <spotbugs.version>4.10.3.0</spotbugs.version>
        <findsecbugs.version>1.14.0</findsecbugs.version>
        <dependency-check.version>12.2.2</dependency-check.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 内部模块统一版本管理（模块间依赖不写版本号） -->
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-provider</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-memory</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-tool</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-web</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-channel-cli</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-storage</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-cli</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oryxos</groupId>
                <artifactId>oryxos-boot</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Spring AI：只用其 Provider 抽象 / 协议转换 / @Tool 的 schema 生成（constitution 原则四，禁用自动 tool 执行） -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring AI Alibaba BOM（含 agent/graph/studio 模块，按需使用；核心 LLM 调用走 spring-ai-core + spring-ai-openai） -->
            <dependency>
                <groupId>com.alibaba.cloud.ai</groupId>
                <artifactId>spring-ai-alibaba-bom</artifactId>
                <version>${spring-ai-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.xerial</groupId>
                <artifactId>sqlite-jdbc</artifactId>
                <version>${sqlite-jdbc.version}</version>
            </dependency>
            <!-- SQLite 方言不在 Hibernate 核心，需显式 pin（Boot 未管理此 artifact 版本） -->
            <dependency>
                <groupId>org.hibernate</groupId>
                <artifactId>hibernate-community-dialects</artifactId>
                <version>${hibernate.version}</version>
            </dependency>
            <dependency>
                <groupId>info.picocli</groupId>
                <artifactId>picocli</artifactId>
                <version>${picocli.version}</version>
            </dependency>
            <dependency>
                <groupId>io.modelcontextprotocol.sdk</groupId>
                <artifactId>mcp</artifactId>
                <version>${mcp-sdk.version}</version>
            </dependency>

            <!-- 工程地基：OpenAPI / 结构化日志 / 监控 -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback-encoder.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>

                <!-- 格式层：Spotless + google-java-format（管缩进/import/换行，apply 一键自动修） -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.version}</version>
                    <configuration>
                        <java>
                            <googleJavaFormat>
                                <version>${google-java-format.version}</version>
                                <style>GOOGLE</style>
                            </googleJavaFormat>
                            <removeUnusedImports/>
                            <importOrder/>
                        </java>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- 兜底：Checkstyle（google_checks.xml） -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${checkstyle.version}</version>
                    <configuration>
                        <configLocation>google_checks.xml</configLocation>
                        <consoleOutput>true</consoleOutput>
                        <failOnViolation>true</failOnViolation>
                        <failsOnError>true</failsOnError>
                        <violationSeverity>warning</violationSeverity>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- 编码规约层：阿里 P3C（挂在 PMD 6 上）。
                     必须用 maven-pmd-plugin 3.21.0（PMD 6.55）：p3c-pmd 2.1.1 编译依赖 PMD 6.15，
                     PMD 7 无法加载 P3C 规则集。 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>${pmd-plugin.version}</version>
                    <configuration>
                        <rulesets>
                            <ruleset>rulesets/java/ali-pmd.xml</ruleset>
                            <ruleset>rulesets/java/ali-concurrent.xml</ruleset>
                            <ruleset>rulesets/java/ali-exception.xml</ruleset>
                            <ruleset>rulesets/java/ali-oop.xml</ruleset>
                            <ruleset>rulesets/java/ali-naming.xml</ruleset>
                        </rulesets>
                        <failOnViolation>true</failOnViolation>
                        <printFailingErrors>true</printFailingErrors>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>com.alibaba.p3c</groupId>
                            <artifactId>p3c-pmd</artifactId>
                            <version>${p3c-pmd.version}</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- 安全扫描：SpotBugs + Find Security Bugs（OWASP Top 10） -->
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs.version}</version>
                    <configuration>
                        <effort>Max</effort>
                        <threshold>Low</threshold>
                        <failOnError>true</failOnError>
                        <plugins>
                            <plugin>
                                <groupId>com.h3xstream.findsecbugs</groupId>
                                <artifactId>findsecbugs-plugin</artifactId>
                                <version>${findsecbugs.version}</version>
                            </plugin>
                        </plugins>
                    </configuration>
                </plugin>

                <!-- 第三方依赖 CVE 扫描：OWASP Dependency-Check（CI 阶段执行，指定 failBuildOnCVSS） -->
                <plugin>
                    <groupId>org.owasp</groupId>
                    <artifactId>dependency-check-maven</artifactId>
                    <version>${dependency-check.version}</version>
                    <configuration>
                        <failBuildOnCVSS>7</failBuildOnCVSS>
                        <formats>
                            <format>HTML</format>
                            <format>JSON</format>
                        </formats>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!-- 激活格式/规约检查随 mvn verify 执行（配置继承自 pluginManagement）。
             SpotBugs 与 Dependency-Check 不在生命周期里绑定（慢且需联网），由 CI 显式跑。 -->
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
