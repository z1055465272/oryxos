/**
 * OryxOS Web 层：统一响应体、全局异常处理、traceId 过滤器、运行信息端点。
 *
 * <p>工程地基部分（统一 ApiResponse / ErrorResponse / GlobalExceptionHandler / MdcFilter / InfoController）；
 * 能力五的业务端点（sessions / agents / profiles / memory / tools / health）在对应 user story 落地。
 */
package com.oryxos.web;
